server:
  port: 8082

spring:
  application:
    name: session

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/local-session?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: No3281792003@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      fail-on-empty-beans: false

logging:
  level:
    org.hibernate.SQL: debug